<h1>Editing Actor #<%= @actor.id %></h1>


<%= form_tag(actor_url(@actor), method: 'patch') do %>

  <div>
    <%= label_tag :name %><br />
    <%= text_field_tag :name, @actor.name %>
  </div>

  
  <div>
    <%= submit_tag "Update Actor" %>
  </div>

<% end %>
